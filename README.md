# face-mask-dectector
😷 AI-powered face mask detection system built with CNN (TensorFlow/Keras). Upload an image and instantly detect whether a person is wearing a surgical mask or not. Trained on a custom dataset with 850+ images.
